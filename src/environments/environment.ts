// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    baseApiUrl: "https://localhost:7176",
    clientRoot : "http://localhost:4200",
    idpAuthority : "http://197.168.1.22:5055/",
    clientId : "FRABackOffice",
    clientScopes: "openid profile email IdentityServerApi ServiceCatalogApi UserMarketApi ApplicationApi ProcessApi AuditorApi FileManagerApi",

  };

  